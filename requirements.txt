numpy>=1.26
scipy>=1.11
pandas>=2.0
requests>=2.31
fastapi>=0.110
# Install the hone package itself so serverless platforms that only run
# `pip install -r requirements.txt` (e.g. Vercel) get it into
# site-packages with its static files, independent of file bundling.
.
